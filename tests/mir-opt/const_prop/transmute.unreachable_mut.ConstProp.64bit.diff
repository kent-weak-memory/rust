- // MIR for `unreachable_mut` before ConstProp
+ // MIR for `unreachable_mut` after ConstProp
  
  fn unreachable_mut() -> ! {
      let mut _0: !;
      let _1: &mut Never;
      let mut _2: &mut Never;
      let mut _3: *const Never;
      scope 1 {
          debug x => _1;
      }
      scope 2 {
      }
  
      bb0: {
          StorageLive(_1);
          StorageLive(_2);
          StorageLive(_3);
-         _3 = const 1_usize as *const Never (PointerFromExposedAddress);
-         _2 = move _3 as &mut Never (Transmute);
+         _3 = const {0x1 as *const Never};
+         _2 = const {0x1 as &mut Never};
          StorageDead(_3);
          _1 = &mut (*_2);
          StorageDead(_2);
          unreachable;
      }
  }
  
