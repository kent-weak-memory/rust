- // MIR for `transmute_to_box_uninhabited` before LowerIntrinsics
+ // MIR for `transmute_to_box_uninhabited` after LowerIntrinsics
  
  fn transmute_to_box_uninhabited() -> ! {
      let mut _0: !;
      let _1: std::boxed::Box<Never>;
      let mut _2: *const Never;
      scope 1 {
          debug x => _1;
      }
  
      bb0: {
          StorageLive(_1);
          StorageLive(_2);
          _2 = const 1_usize as *const Never (PointerFromExposedAddress);
-         _1 = transmute::<*const Never, Box<Never>>(move _2) -> [return: bb1, unwind unreachable];
+         _1 = move _2 as std::boxed::Box<Never> (Transmute);
+         goto -> bb1;
      }
  
      bb1: {
          StorageDead(_2);
          unreachable;
      }
  }
  
